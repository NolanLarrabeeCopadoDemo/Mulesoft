<?xml version="1.0" encoding="UTF-8"?>

<mule xmlns="http://www.mulesoft.org/schema/mule/core"
      xmlns:munit="http://www.mulesoft.org/schema/mule/munit"
      xmlns:http="http://www.mulesoft.org/schema/mule/http"
      xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
      xsi:schemaLocation="http://www.mulesoft.org/schema/mule/core
                          http://www.mulesoft.org/schema/mule/core/current/mule.xsd
                          http://www.mulesoft.org/schema/mule/http
                          http://www.mulesoft.org/schema/mule/http/current/mule-http.xsd
                          http://www.mulesoft.org/schema/mule/munit
                          http://www.mulesoft.org/schema/mule/munit/current/mule-munit.xsd">

    <!-- Test Suite for API 1 -->
    <munit:test name="api1TestSuite">
        <munit:config name="httpListenerConfig_API1"/>
        <munit:config name="httpRequestConfig_API1"/>
        <munit:mocking-config>
            <munit:when messageProcessor="http:listener" config-ref="httpListenerConfig_API1"/>
        </munit:mocking-config>

        <munit:execution>
            <http:request config-ref="httpRequestConfig_API1" path="/api1" method="GET"/>
        </munit:execution>

        <munit:assert-that expression="#[payload]" is="API 1 Response"/>
    </munit:test>

    <!-- Test Suite for API 2 -->
    <munit:test name="api2TestSuite">
        <munit:config name="httpListenerConfig_API2"/>
        <munit:config name="httpRequestConfig_API2"/>
        <munit:mocking-config>
            <munit:when messageProcessor="http:listener" config-ref="httpListenerConfig_API2"/>
        </munit:mocking-config>

        <munit:execution>
            <http:request config-ref="httpRequestConfig_API2" path="/api2" method="GET"/>
        </munit:execution>

        <munit:assert-that expression="#[payload]" is="API 2 Response"/>
    </munit:test>

</mule>

